# 💈 پکیج انتقال — Logic Salon (آرایشگاه واقعی، آلمان)

> این پروژه **تازه شروع شده** — زیرساخت فنی واقعی است، ولی محتوای واقعی سالن (نام، آدرس، خدمات، قیمت) هنوز جای `__PLACEHOLDER__` است. اول باید این‌ها پر شوند.

---

## ✅ آنچه واقعاً ساخته شده (کار می‌کند)

### Supabase (به‌اشتراک با پروژهٔ Logic Style — چون سهمیهٔ رایگان پر بود)
- **Project ID:** `owgfhhkmlkdpjqlwavsx` (همان Logic Style، ولی جداول مجزا با پیشوند `salon_`)
- **URL:** `https://owgfhhkmlkdpjqlwavsx.supabase.co`
- **Publishable Key:** `sb_publishable_xGeaZikMQI8MclH3CnClBg_INoBJ7F-`
- **جداول:** `salon_appointments` (نوبت‌ها)، `salon_expenses` (هزینه‌ها)، `salon_feedback` (بازخورد)
- ⚠️ **توصیه:** وقتی سالن واقعاً فعال شد، بهتر است پروژهٔ Supabase مستقل بسازی (پلن رایگان کامل بود؛ یا یکی از پروژه‌های قدیمی pause شود، یا آپگرید شود).

### فایل‌های سایت (پیوست) — با placeholder
- `index.html` — سایت آرایشگاه، فرم رزرو نوبت → می‌رود در `salon_appointments`
- `admin.html` — پنل مدیریت نوبت‌ها

**جاهایی که باید پر کنی (grep کن دنبال `__..__`):**
`__SALON_NAME__`, `__SALON_ADDRESS__`, `__SALON_PHONE__`, `__HERO_TITLE__`, `__HERO_SUBTITLE__`, `__SERVICE_1/2/3_NAME__`, `__SERVICE_1/2/3_PRICE__`

---

## ❌ آنچه هنوز ساخته نشده (نیاز به اطلاعات/تصمیم تو)

| بخش | چه چیزی لازم است |
|-----|-------------------|
| **گیت‌هاب** | یک ریپوی جدید (توکن فعلی منقضی — باید تازه بسازی) |
| **Vercel/دامنه** | باید دامنه بخری یا زیردامنه‌ای از logicstyle-salon.de اختصاص دهی |
| **اینستاگرام سالن** | نیاز به اکانت اینستاگرام مخصوص Logic Salon + توکن Graph API (این با اکانت کلبه/Logic Style فرق دارد) |
| **تلگرام سالن** | نیاز به بات تلگرام جدید (BotFather) اگر می‌خواهی جدا از بقیه باشد |
| **محتوای واقعی** | نام دقیق سالن، آدرس کامل، لیست خدمات و قیمت واقعی (یورو)، عکس‌های سالن |
| **workflow تحلیل ۶P/۲۴P** | بعد از اینکه چند هفته داده واقعی جمع شد، می‌توان مثل کلبه ساخت |

---

## 🚀 پرامپت شروع چت جدید

```
سلام کلود. می‌خواهم پروژهٔ «Logic Salon» (آرایشگاه واقعی در آلمان) را از این نقطه 
ادامه بدهم. فایل LOGIC_SALON_HANDOFF.md را ضمیمه کردم — زیرساخت اولیه (Supabase، 
سایت با placeholder) آماده است، ولی محتوای واقعی هنوز خالی است.

اطلاعات واقعی سالن که الان می‌دهم: [اینجا نام/آدرس/خدمات/قیمت را بنویس]

لطفاً:
۱) فایل‌های index.html و admin.html را با این اطلاعات واقعی پر کن (placeholder ها 
   را با str_replace عوض کن، نه بازنویسی کامل).
۲) بگو برای گیت‌هاب و اینستاگرام/تلگرام چه چیزی از من لازم داری.
```
